[project]
name = "awcee-autoagent"
version = "1.0.0"
description = "AWCEE agents — architecture kevinrgu/autoagent"
requires-python = ">=3.12"
dependencies = [
    "claude-agent-sdk",
    "python-dotenv",
    "pandas",
    "openpyxl",
    "numpy",
    "harbor",
]
